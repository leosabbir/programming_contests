apply plugin: "java"

processResources {
    from 'res'
}

sourceSets {
    main.java.srcDir "src"
}

// jar {
// manifest.attributes "Main-Class": "sample.Main"
// }

task run(type: JavaExec, dependsOn: classes) {
    main 'LargestSumSubarray'
    classpath  sourceSets.main.runtimeClasspath
}