apply plugin: "java"

processResources {
    from 'res'
}

sourceSets {
    main.java.srcDir "src"
}

// jar {
// manifest.attributes "Main-Class": "sample.Main"
// }

task run(type: JavaExec, dependsOn: classes) {
    main 'SelectKthMinium'
    classpath  sourceSets.main.runtimeClasspath
}